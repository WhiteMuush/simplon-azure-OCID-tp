default:
  image: docker:27.3
  services: 
    - docker:27.3-dind

variables:
  DOCKER_TLS_CERTDIR: "/certs"

stages:
  - build

build-job:
  stage: build
  script:
    - docker build -t python-app .
    - docker run -d -p 8080:8080 --name CI python-app
    - sleep 3
    - docker stop CI